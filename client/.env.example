# Client environment variables (Vite-baked)
# In development and unified deployment, requests use relative /api paths.
# If hosting client separately on Vercel without proxying, set the full API origin:
# VITE_API_URL=https://your-api.onrender.com
